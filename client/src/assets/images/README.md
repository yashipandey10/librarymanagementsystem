# Images Directory

This directory contains static images used in the frontend application.

## Default Images

- `default-book.png` - Default placeholder image for books without cover images

## Usage

Import images from this directory using:

```javascript
import defaultBookImage from '../assets/images/default-book.png';
```

Or reference them directly in public folder if placed there.
